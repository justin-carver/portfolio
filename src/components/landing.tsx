'use client';

const Landing = () => {
    return (
        <div className="flex">
            # **THIS** _IS_ ~~SOME~~ MARKDOWN, is it possible that this is going to wrap correctly?
        </div>
    );
};

export default Landing;
